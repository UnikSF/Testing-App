[pytest]
testpaths = .
addopts = -v --tb=short
markers =
    docker: tests that require Docker / docker compose
    gpu: tests that require an NVIDIA GPU
    integration: tests that hit live external services (Ollama, model APIs)
